name: pages-health-check

on:
  workflow_run:
    workflows: [deploy-github-pages]
    types:
      - completed

jobs:
  ping:
    runs-on: ubuntu-latest
    if: github.event.workflow_run.conclusion == 'success'
    steps:
      - run: echo "Deployment healthy"
